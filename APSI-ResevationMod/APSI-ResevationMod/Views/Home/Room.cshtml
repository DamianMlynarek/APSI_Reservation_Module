@model APSI_ResevationMod.Models.DetailsRoom

@{
    ViewBag.Title = "Room data";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>


<div class="row page-box">
    <div class="col-sm-12">


        @using (Html.BeginForm("Apply"))
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                @Html.LabelFor(m => m.Room.RoomNumber)
                <div>
                    @Html.DisplayFor(m => m.Room.RoomNumber, new { htmlAttributes = new { @class = "form-control" } })
                    <p>1234</p>
                </div>
            </div>

            @Html.AntiForgeryToken()
            <div class="form-group">
                @Html.LabelFor(m => m.RoomReservations)
                <div>
                    @Html.DisplayFor(m => m.RoomReservations, new { htmlAttributes = new { @class = "form-control" } })
                    
                </div>
            </div>
        }
    </div>
</div>
